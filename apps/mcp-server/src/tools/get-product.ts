import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerGetProductTool(_server: McpServer): void {
  // TODO(SANGYOON): Register get_product and delegate to packages/commerce.
}
