import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerGetPaymentStatusTool(_server: McpServer): void {
  // TODO(SANGYOON): Register get_payment_status and delegate to packages/commerce.
}
