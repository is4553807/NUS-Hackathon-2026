import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerInitiatePaymentTool(_server: McpServer): void {
  // TODO(SANGYOON): Register initiate_payment and delegate to packages/commerce.
}
