import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerSearchProductsTool(_server: McpServer): void {
  // TODO(SANGYOON): Register search_products and delegate to packages/commerce.
}
