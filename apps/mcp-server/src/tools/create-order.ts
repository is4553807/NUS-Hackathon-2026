import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerCreateOrderTool(_server: McpServer): void {
  // TODO(SANGYOON): Register create_order and delegate to packages/commerce.
}
