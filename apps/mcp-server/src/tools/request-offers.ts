import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerRequestOffersTool(_server: McpServer): void {
  // TODO(SANGYOON): Register request_offers and delegate to packages/commerce.
}
